#!/usr/bin/env bash

# Exit immediately if there is an error
set -e

export FAST_BUILDS=true;

# echo out each line of the shell as it executes
set -x

bundle check || bundle install

bundle exec jekyll serve --watch --incremental  --limit_posts 1
